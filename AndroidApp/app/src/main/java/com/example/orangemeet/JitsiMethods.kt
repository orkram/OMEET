package com.example.orangemeet

import androidx.fragment.app.Fragment
import androidx.navigation.fragment.findNavController

class JitsiMethods {
    companion object{
        fun getIntoCall(callId: String)
        {
            if (UserInfo.isInConference == false)
            {

            }
        }
    }
}